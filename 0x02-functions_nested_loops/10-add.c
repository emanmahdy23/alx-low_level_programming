#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result.
 *@a: first input
 *@b: second input
 * Return: sum a + b
 */
int add(int a, int b)
{
int sum = a + b;
return (sum);
}
