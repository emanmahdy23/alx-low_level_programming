#include "main.h"
/**
*  print_rev - function that prints a string in reverse
* @s: the pointer to the string
*/
void print_rev(char *s)
{
int num = 0;
while (s[num])
{
num++
}
while (num--)
{
_putchar(s[num]);
}
_putchar('\n');
}
