#include "main.h"
#include <stdio.h>
/**
 * int mul(int a, int b) -  function that multiplies two integers.
 * @a: the first number
 * @b: the second number
 * Return: always (0)
 */
int mul(int a, int b)
{
return (a * b);
}
